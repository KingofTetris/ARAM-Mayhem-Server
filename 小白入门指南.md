# ARAM Mayhem Server - 后端服务

> 海克斯大乱斗信息差助手 - Spring Boot 后端服务
>
> **从零开始的小白入门指南**

[![GitHub](https://img.shields.io/badge/github-KingofTetris/ARAM--Mayhem--Server-blue?logo=github)
[![License](https://img.shields.io/badge/license-MIT-green)
[![Java Version](https://img.shields.io/badge/java-21-orange)
[![Spring Boot](https://img.shields.io/badge/spring%20boot-3.3.5-brightgreen)

---

## 📖 目录

- [这是什么项目？](#这是什么项目)
- [电脑需要什么配置？](#电脑需要什么配置)
- [第一步：安装开发工具](#第一步安装开发工具)
- [第二步：安装数据库和缓存](#第二步安装数据库和缓存)
- [第三步：下载源代码](#第三步下载源代码)
- [第四步：配置项目](#第四步配置项目)
- [第五步：启动服务](#第五步启动服务)
- [第六步：测试 API](#第六步测试-api)
- [常见问题](#常见问题)
- [项目结构说明](#项目结构说明)

---

## 🎯 这是什么项目？

**ARAM Mayhem Server** 是给 [Android 应用](https://github.com/KingofTetris/ARAM-Mayhem-Android) 提供数据的后端服务。

**简单理解：** 手机 App 不能自己获取数据，需要问后端要。后端就像一个「数据仓库管理员」，App 发请求问它要数据，它就去数据库里找然后返回给 App。

**主要功能：**
- 🔐 **用户登录注册**：安全的账号系统
- 👤 **用户管理**：个人信息维护
- 📊 **英雄数据**：提供英雄信息
- 💎 **符文数据**：提供强化符文信息
- 🎮 **社区功能**：策略发布、评论、点赞
- 📢 **公告系统**：发布系统公告

---

## 💻 电脑需要什么配置？

### 最低配置

| 项目 | 要求 |
|------|------|
| 操作系统 | Windows 10/11 或 macOS 10.14+ |
| 内存 | 8GB RAM（推荐 16GB） |
| 硬盘 | 至少 5GB 可用空间 |
| 处理器 | 任意多核处理器 |

### 开发工具

- **JDK 21** - Java 开发工具包（必须）
- **Maven 3.9+** - 项目构建工具（必须）
- **MySQL 8.0+** - 数据库（必须）
- **Redis** - 缓存（可选，项目可跑但性能下降）
- **IntelliJ IDEA** - Java 开发IDE（推荐）
- **Git** - 代码版本管理工具（推荐）

---

## 📥 第一步：安装开发工具

### 1.1 安装 JDK 21

1. 访问 [Eclipse Temurin 下载页面](https://adoptium.net/temurin/releases/?version=21)
2. 下载 **JDK 21** 版本（选择 Windows x64 或 macOS x64）
3. 运行安装程序，一路点击「下一步」即可
4. 验证安装：打开命令行，输入 `java -version`，应该显示 `21.x.x`

### 1.2 安装 Maven

**Windows:**

1. 访问 [Maven 下载页面](https://maven.apache.org/download.cgi)
2. 下载 `apache-maven-3.9.x-bin.zip`
3. 解压到 `D:\apache-maven`（或你喜欢的目录）
4. 添加环境变量：
   - 右键「此电脑」→「属性」→「高级系统设置」→「环境变量」
   - 在「系统变量」中找到 `Path`，点击「编辑」
   - 添加 `D:\apache-maven\bin`
5. 验证：打开新的命令行窗口，输入 `mvn -version`

**macOS/Linux:**
```bash
brew install maven
# 或
sudo apt-get install maven
```

### 1.3 安装 IntelliJ IDEA（推荐）

1. 访问 [IntelliJ IDEA 下载页面](https://www.jetbrains.com/idea/download/)
2. 下载 Community（免费版）或 Ultimate（付费版）
3. 安装并启动

---

## 🗄️ 第二步：安装数据库和缓存

### 2.1 安装 MySQL 8.0

**方式一：使用 phpStudy（推荐新手）**

1. 下载 [phpStudy](https://www.xp.cn/download.html)
2. 安装后启动
3. 点击「套件」→ 选择「MySQL」和「phpMyAdmin」
4. 点击「启动」
5. 打开浏览器访问 http://localhost/phpmyadmin

**方式二：直接安装 MySQL**

1. 下载 [MySQL Community Server](https://dev.mysql.com/downloads/mysql/)
2. 选择 Windows Installer 或 DMG Package
3. 安装时记住设置的 root 密码
4. 安装完成后，使用 MySQL Workbench 或命令行连接

**创建数据库：**
```sql
-- 登录 MySQL
mysql -u root -p

-- 创建数据库
CREATE DATABASE aram_mayhem CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

-- 验证
SHOW DATABASES;
```

### 2.2 安装 Redis（可选但推荐）

Redis 用于缓存数据，可以提升访问速度。

**Windows:**

1. 下载 [Redis for Windows](https://github.com/tporadowski/redis/releases)
2. 解压到目录
3. 运行 `redis-server.exe`

**macOS:**
```bash
brew install redis
brew services start redis
```

**Linux:**
```bash
sudo apt-get install redis-server
sudo systemctl start redis
```

---

## 📂 第三步：下载源代码

### 方法一：使用 Git 下载（推荐）

1. 在电脑找个放代码的地方（比如 `D:\workspace`）
2. 右键选择「Git Bash Here」
3. 输入：
   ```bash
   git clone https://github.com/KingofTetris/aram-server.git
   ```
4. 等待下载完成

### 方法二：直接下载

1. 访问 [GitHub 项目页面](https://github.com/KingofTetris/aram-server)
2. 点击「Code」→「Download ZIP」
3. 解压到你想放的位置

---

## ⚙️ 第四步：配置项目

### 4.1 打开项目

1. 启动 IntelliJ IDEA
2. 选择「Open」
3. 选择 `aram-server` 文件夹
4. 等待 Maven 自动下载依赖（可能需要 5-15 分钟）

### 4.2 配置数据库连接

1. 打开 `src/main/resources/application.yml`
2. 修改数据库连接信息：

```yaml
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/aram_mayhem?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root          # 你的 MySQL 用户名
    password: your_password  # 你的 MySQL 密码
```

### 4.3 配置 Redis（可选）

如果没安装 Redis，修改 `application.yml`：

```yaml
spring:
  data:
    redis:
      host: localhost
      port: 6379
      # 如果设置了密码，取消下面这行注释并填写
      # password: your_redis_password
```

**注意：** 如果完全不启动 Redis，服务启动时会报错。确保 Redis 正在运行或临时注释掉 Redis 相关配置。

### 4.4 配置端口

默认端口是 `8080`。如果要改：

```yaml
server:
  port: 8080
```

---

## ▶️ 第五步：启动服务

### 5.1 启动 MySQL 和 Redis

确保 MySQL 和 Redis 服务正在运行。

### 5.2 启动后端服务

**方式一：使用 IDEA（推荐）**

1. 在 IDEA 中打开项目
2. 找到 `AramServerApplication.java`（在 src/main/java/com/aram/mayhem/ 下）
3. 右键点击文件，选择「Run 'AramServerApplication'」
4. 等待启动，看到 `Started AramServerApplication` 表示成功

**方式二：使用 Maven**

```bash
cd aram-server
mvn spring-boot:run
```

**方式三：使用命令行**

```bash
cd aram-server
mvn clean package
java -jar target/aram-server-1.0.0.jar
```

### 5.3 验证启动成功

打开浏览器访问：http://localhost:8080

如果看到 JSON 响应说明启动成功：
```json
{"code":200,"message":"success","data":"ARAM Mayhem Server is running!"}
```

---

## 🧪 第六步：测试 API

### 6.1 查看 API 文档

启动服务后访问：
- Swagger UI: http://localhost:8080/swagger-ui.html
- OpenAPI 文档: http://localhost:8080/v3/api-docs

### 6.2 测试注册

```bash
curl -X POST http://localhost:8080/api/auth/register \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","password":"123456","email":"test@example.com"}'
```

### 6.3 测试登录

```bash
curl -X POST http://localhost:8080/api/auth/login \
  -H "Content-Type: application/json" \
  -d '{"username":"testuser","password":"123456"}'
```

成功后会返回 JWT token，复制这个 token 后续使用。

### 6.4 测试获取英雄列表

```bash
curl http://localhost:8080/api/heroes \
  -H "Authorization: Bearer YOUR_TOKEN_HERE"
```

---

## ❓ 常见问题

### Q1: 启动时报「Communications link failure」

**原因：** 找不到 MySQL 数据库

**解决方法：**
1. 确保 MySQL 服务已启动
2. 检查 `application.yml` 中的数据库地址、用户名、密码是否正确
3. 检查数据库 `aram_mayhem` 是否已创建

### Q2: 启动时报「Cannot connect to Redis」

**原因：** Redis 服务未启动

**解决方法：**
1. 启动 Redis 服务
2. 或在 `application.yml` 中确保 Redis 配置正确

### Q3: 端口被占用

**原因：** 8080 端口被其他程序占用

**解决方法：**
1. 查找占用端口的程序：`netstat -ano | findstr 8080`
2. 关闭该程序或修改 `application.yml` 中的端口

### Q4: Maven 下载依赖很慢

**解决方法：**
1. 配置阿里云镜像（推荐）
2. 在 `pom.xml` 或 `settings.xml` 中添加：

```xml
<mirrors>
  <mirror>
    <id>aliyun</id>
    <name>Aliyun Maven</name>
    <url>https://maven.aliyun.com/repository/public</url>
    <mirrorOf>central</mirrorOf>
  </mirror>
</mirrors>
```

### Q5: 编译报错「Java version mismatch」

**原因：** JDK 版本不对

**解决方法：**
1. 确保安装了 JDK 21
2. 在 IDEA 中设置：File → Project Structure → Project → SDK 选择 21
3. 在 IDEA 中设置：File → Project Structure → Modules → Language Level 选择 21

### Q6: 数据库表没有创建

**原因：** 项目没有配置自动建表

**解决方法：**
项目使用 MyBatis Plus，需要手动执行 SQL 初始化脚本或让项目自动创建表（如果配置了的话）。

---

## 📐 项目结构说明

```
aram-server/
├── src/main/java/com/aram/mayhem/
│   ├── AramServerApplication.java    # 启动类
│   ├── common/                        # 公共类
│   │   ├── BusinessException.java     # 业务异常
│   │   ├── GlobalExceptionHandler.java # 全局异常处理
│   │   └── Result.java                 # 统一响应格式
│   ├── config/                        # 配置类
│   │   ├── CorsConfig.java            # 跨域配置
│   │   ├── RedisConfig.java           # Redis配置
│   │   └── SecurityConfig.java        # 安全配置
│   ├── controller/                   # 控制器（接收请求）
│   ├── dto/                          # 数据传输对象
│   ├── entity/                       # 实体类（对应数据库表）
│   ├── mapper/                       # 数据库映射
│   ├── security/                     # 安全相关（JWT等）
│   └── service/                      # 业务逻辑
└── src/main/resources/
    └── application.yml              # 配置文件
```

**请求流程：**
```
用户请求 → Controller → Service → Mapper → 数据库
                              ↓
                           Redis缓存
```

---

## 🔧 技术栈

| 技术 | 用途 |
|------|------|
| Java 21 | 编程语言 |
| Spring Boot 3.3.5 | Web 框架 |
| MyBatis Plus | 数据库 ORM |
| Spring Security + JWT | 安全认证 |
| MySQL | 主数据库 |
| Redis | 缓存 |
| SpringDoc OpenAPI | API 文档 |

---

## 📁 配套项目

- [Android 客户端](https://github.com/KingofTetris/ARAM-Mayhem-Android) - 手机 App

---

## 📄 许可证

本项目采用 MIT 许可证。

---

## 👨‍💻 作者

- **GitHub**: [KingofTetris](https://github.com/KingofTetris)

---

> 如果觉得有帮助，请给项目一个 ⭐ Star！
